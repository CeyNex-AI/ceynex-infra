# Copy to `.env`, fill in real values.

# The BACKEND VM's internal IP — get with:
#   gcloud compute instances describe backend --zone=asia-south1-a \
#     --format='get(networkInterfaces[0].networkIP)'
BACKEND_INTERNAL_IP=10.x.x.x
