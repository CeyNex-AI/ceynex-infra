# Filled-in environment files carry real credentials. Only the .example files
# are tracked.
.env
*/.env

.DS_Store

# Local working notes — the tracked guidance is README.md and DEPLOY_ORDER.md.
/CLAUDE.md
